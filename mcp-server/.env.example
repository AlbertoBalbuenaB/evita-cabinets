# Copy this file to `.env` and fill in real values.
# These env vars can also be provided via the "env" block in
# claude_desktop_config.json / .claude/mcp.json — see README.md.

SUPABASE_URL=https://rludrzyrpsotvzizlztg.supabase.co

# Service role key (bypasses RLS). Get it from:
#   Supabase Dashboard -> Project Settings -> API -> service_role secret
# SECURITY: never commit the real value. .env is gitignored.
SUPABASE_SERVICE_ROLE_KEY=
